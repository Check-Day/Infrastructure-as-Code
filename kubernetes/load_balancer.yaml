---
apiVersion: v1
kind: Service
metadata:
  name: checkday-public-load-balancer
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-type: nlb
spec:
  type: LoadBalancer
  selector:
    app: checkday-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5969